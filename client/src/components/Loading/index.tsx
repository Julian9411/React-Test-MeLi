import ReactLoading from "react-loading";

export const Loading = () => (
  <div className="items_container_pages container-loading">
    <ReactLoading className="loading" type="bubbles" color="#666" />
  </div>
);
